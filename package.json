{
  "name": "rustymirosquid",
  "version": "1.0.0",
  "description": "RustyMiroSquid - High-Performance Multi-Agent Investment Simulation Engine",
  "scripts": {
    "setup:backend": "cd backend && uv sync",
    "setup:frontend": "cd frontend && bun install",
    "setup:all": "bun run setup:backend && bun run setup:frontend",
    "dev": "concurrently -n \"backend,frontend\" -c \"green,cyan\" \"bun run backend\" \"bun run frontend\"",
    "backend": "cd backend && uv run python run.py",
    "frontend": "cd frontend && bun run dev -- --host",
    "build": "cd frontend && bun run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "AGPL-3.0"
}
