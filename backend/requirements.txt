# ===========================================
# MiroShark Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM Related =============
# OpenAI SDK (unified LLM calls using OpenAI format)
openai>=1.0.0

# ============= Neo4j =============
neo4j>=5.15.0

# ============= HTTP Requests =============
# For Ollama embedding API
requests>=2.31.0

# ============= OASIS Social Media Simulation (Wonderwall fork) =============
# Wonderwall (oasis package) is bundled directly in backend/oasis/
# No separate install needed — camel-oasis is no longer a pip dependency
camel-ai==0.2.78

# Wonderwall runtime dependencies (needed by recsys, agent_graph, agents_generator)
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
torch>=2.0.0
sentence-transformers>=2.2.0
igraph>=0.11.0

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 encoded text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Trading API Integration =============
alpaca-py>=0.30.0
ib_insync>=0.9.86

# ============= Utility Libraries =============
# Environment variable loading
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
# Data Retrieval & Triggers
yfinance>=0.2.36
feedparser>=6.0.10

# ============= Development / Testing =============
pytest>=8.0.0
pytest-asyncio>=0.23.0
